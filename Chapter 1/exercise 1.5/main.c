#include <stdio.h>
#include <stdlib.h>

//1.5

int main()
{
    /*
    //example(4) of unary operator
    int i;

    i=10;
    i=-i;
    printf("This is i:%d\n",i);
    */


/*
    //1.write  a program to calculate volume of a cube.Have the program prompt the user for each dimension

    float length,width,height,volume;

    printf("Enter length:");
    scanf("%f",&length);

    printf("Enter width:");
    scanf("%f",&width);

    printf("Enter height:");
    scanf("%f",&height);

    volume=length*width*height;

    printf("Volume of the cube:%f",volume);

*/



    //2.number of seconds in a year

    int seconds;
    seconds=365*24*60*60;

    printf("Number of seconds in a year:%d",seconds);


    return 0;
}
